{
  "name": "desktop-auto-testing",
  "version": "1.0.0",
  "description": "Playwright automation tests for agent desktop application",
  "scripts": {
    "test": "npx playwright test",
    "test:badge-fix": "DESKTOP_PATH=/desktopv2 npx playwright test tests/02-badge-bug.spec.ts",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
